
- More documentation
- Work out platform dependecy kinks (win paths -> normal paths)
- when using a md5sum file: option for whether to list all the files in one metalink, or make
	separate metalinks for each line. (Currently an ugly separate script, see example/separatemd5.sh)
- Support for openssl like hash lists

Also see `sh todo.sh`
