# Defaults for metalink-checker initscript
# sourced by /etc/init.d/metalink-checker
# installed at /etc/default/metalink-checker by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
